<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#badc58"
    android:orientation="vertical"
    tools:context="corp.poopapps.afinal.MainActivity">

    <ImageView
        android:id="@+id/bannerRA"
        android:layout_width="match_parent"
        android:layout_height="90dp"
        android:layout_marginTop="30dp"
        android:src="@drawable/camarabanner" />

    <ImageView
        android:id="@+id/bannerEscultores"
        android:layout_width="match_parent"
        android:layout_height="90dp"
        android:layout_below="@+id/bannerRA"
        android:layout_marginTop="30dp"
        android:src="@drawable/escultoresbanner" />

    <ImageView
        android:id="@+id/bannerMapa"
        android:layout_width="match_parent"
        android:layout_height="90dp"
        android:layout_below="@+id/bannerEscultores"
        android:layout_marginTop="30dp"
        android:src="@drawable/mapabanner" />

    <ImageView
        android:id="@+id/bannerBuscar"
        android:layout_width="match_parent"
        android:layout_height="90dp"
        android:layout_below="@+id/bannerMapa"
        android:layout_marginTop="30dp"
        android:src="@drawable/buscarbanner" />

</RelativeLayout>
